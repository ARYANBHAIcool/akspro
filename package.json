{
  "name": "aryanstreams-global",
  "version": "1.0.0",
  "description": "AryanStreams Global - Standalone Live Sports & StreamCorner Multi-Server Streaming Web App",
  "private": true,
  "scripts": {
    "build": "echo 'Pages Functions Ready'",
    "deploy": "wrangler pages deploy . --project-name=asppv"
  }
}
